Place `muscle_model.onnx` in this folder.

Expected files:
- `public/model/muscle_model.onnx`
- `public/model/class_labels.json`

Run the converter script from project root to generate both files:

`python3 scripts/convert_model_to_onnx.py`
